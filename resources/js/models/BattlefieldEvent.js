export default class BattlefieldEvent {

    constructor({magnitude, xPosition, yPosition}) {
        this.magnitude = magnitude;
        this.xPosition = xPosition;
        this.yPosition = yPosition;
    }
}
