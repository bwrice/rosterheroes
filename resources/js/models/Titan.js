export default class Titan {

    constructor({uuid, slug, name, combatPositionID}) {
        this.uuid = uuid;
        this.slug = slug;
        this.name = name;
        this.combatPositionID = combatPositionID;
    }

}
